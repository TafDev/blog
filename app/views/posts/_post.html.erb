<%= form_for @post do |f| %>
    <%= f.text_field :title %>
    <%= f.text_area :body %>
    <%= f.fields_for :comments do |builder| %>
        <%= render 'comment', f: builder %>
    <% end %>
    <%= f.submit %>
<% end %>
